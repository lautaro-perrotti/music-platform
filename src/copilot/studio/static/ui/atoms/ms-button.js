import { define, attr, bool, emit } from '../lib/define.js';
define('ms-button', el => `<button type="button" class="button ${attr(el,'variant','secondary')} ${attr(el,'size','md')}" ${bool(el,'disabled')?'disabled':''}>${bool(el,'loading')?'…':el.innerHTML || attr(el,'label','Action')}</button>`, `.button{min-height:32px;padding:0 12px;border:1px solid var(--ms-line-strong);border-radius:var(--ms-radius-control);background:var(--ms-control);font-weight:500}.button.primary{background:var(--ms-ivory);border-color:var(--ms-ivory);color:var(--ms-shell);font-weight:650}.button.ghost{background:transparent;border-color:transparent;color:var(--ms-muted)}.button.danger-outline{background:transparent;border-color:color-mix(in srgb,var(--ms-danger) 50%,var(--ms-line));color:var(--ms-danger)}.button.sm{min-height:28px;padding:0 9px;font-size:12px}`, {connected: el => el.shadowRoot.querySelector('button').addEventListener('click',()=>{if(el.dataset.action)emit(el,el.dataset.action,{value:el.textContent.trim()});})});
