import { define, attr, bool, emit } from '../lib/define.js';
define('ms-switch', el => `<button class="switch ${bool(el,'on')?'on':''}" role="switch" aria-checked="${bool(el,'on')}"><i></i></button>`, `.switch{width:32px;height:20px;padding:2px;border:0;border-radius:10px;background:var(--ms-line-strong);display:flex}.switch.on{background:var(--ms-cue);justify-content:flex-end}.switch i{width:16px;height:16px;border-radius:50%;background:var(--ms-ivory)}` , {connected: el => el.shadowRoot.querySelector('button').addEventListener('click',()=>{el.toggleAttribute('on');emit(el,'ms-change',{on:bool(el,'on')});el.paint();})});
