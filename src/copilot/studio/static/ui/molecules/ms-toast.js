import { define, attr } from '../lib/define.js'; define('ms-toast', el => `<div class="toast"><ms-status state="${attr(el,'state','succeeded')}"></ms-status><span>${attr(el,'message','Saved')}</span></div>`, `.toast{display:flex;align-items:center;gap:10px;padding:12px 14px;border:1px solid var(--ms-line-strong);border-radius:var(--ms-radius-control);background:var(--ms-raised);box-shadow:var(--ms-shadow-overlay)}`);
