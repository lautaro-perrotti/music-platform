import { define, attr } from '../lib/define.js'; define('ms-plan-row', el => `<div class="plan"><ms-role-dot role="${attr(el,'role','other')}"></ms-role-dot><span>${attr(el,'role','Element')}</span><b>${attr(el,'value','planned')}</b></div>`, `.plan{display:flex;align-items:center;gap:8px;padding:9px 0;border-bottom:1px solid var(--ms-line)}.plan b{margin-left:auto;font:11px var(--ms-font-mono);color:var(--ms-faint)}`);
