import { define, attr } from '../lib/define.js'; define('ms-fact-row', el => `<div class="fact"><span>${attr(el,'label','Fact')}</span><b>${attr(el,'value','—')}</b></div>`, `.fact{display:flex;justify-content:space-between;gap:12px;padding:8px 0;border-bottom:1px solid var(--ms-line);font-size:12px}.fact span{color:var(--ms-faint)}.fact b{font:11px var(--ms-font-mono);font-weight:500}`);
