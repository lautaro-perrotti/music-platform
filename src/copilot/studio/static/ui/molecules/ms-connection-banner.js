import { define, attr } from '../lib/define.js'; define('ms-connection-banner', el => `<div class="banner"><span class="dot ${attr(el,'state','disconnected')}"></span><span>${attr(el,'message','Ableton status is read-only until a verified session is ready.')}</span></div>`, `.banner{display:flex;align-items:center;gap:8px;padding:9px 12px;background:var(--ms-raised);border-bottom:1px solid var(--ms-line);color:var(--ms-muted);font-size:12px}.dot{width:7px;height:7px;border-radius:50%;background:var(--ms-warning)}.connected{background:var(--ms-success)}`);
