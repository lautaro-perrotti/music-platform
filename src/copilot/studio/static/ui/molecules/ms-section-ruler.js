import { define, attr } from '../lib/define.js'; define('ms-section-ruler', el => `<div class="ruler">${(attr(el,'sections','Intro,Groove,Drop,Break,Outro').split(',')).map((s,i)=>`<span style="left:${i*20}%">${s}</span>`).join('')}</div>`, `.ruler{height:24px;position:relative;border-bottom:1px solid var(--ms-line);color:var(--ms-faint);font:10px var(--ms-font-mono)}span{position:absolute;padding-left:6px;border-left:1px solid var(--ms-line-strong)}`);
