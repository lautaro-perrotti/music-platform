import { define, attr } from '../lib/define.js'; import { icon } from '../lib/icons.js'; define('ms-nav-item', el => `<a href="${attr(el,'href','#')}" class="nav ${el.hasAttribute('active')?'active':''}">${icon(attr(el,'icon','wave'),16)}<span>${attr(el,'label',el.textContent||'Item')}</span>${attr(el,'badge','')?`<b>${attr(el,'badge')}</b>`:''}</a>`, `.nav{height:34px;padding:0 10px;display:flex;align-items:center;gap:10px;border-radius:var(--ms-radius-control);color:var(--ms-muted);text-decoration:none}.nav.active,.nav:hover{background:var(--ms-control);color:var(--ms-text)}.nav b{margin-left:auto;font-size:10px;color:var(--ms-cue)}`);
