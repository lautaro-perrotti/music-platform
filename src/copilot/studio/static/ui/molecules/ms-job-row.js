import { define, attr } from '../lib/define.js'; define('ms-job-row', el => `<div class="job"><div><strong>${attr(el,'title','Generation')}</strong><small>${attr(el,'meta','No provider selected')}</small></div><ms-status state="${attr(el,'state','queued')}"></ms-status></div>`, `.job{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 0;border-bottom:1px solid var(--ms-line)}small{display:block;color:var(--ms-faint);font-size:11px;margin-top:3px}`);
