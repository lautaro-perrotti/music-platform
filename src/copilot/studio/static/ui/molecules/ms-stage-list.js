import { define, attr } from '../lib/define.js'; const stages=['Brief','Generate','Review','Keep','Apply']; define('ms-stage-list', el => `<ol class="stages">${stages.map((s,i)=>`<li class="${i<=Number(attr(el,'active','0'))?'done':''}"><span>${i+1}</span>${s}</li>`).join('')}</ol>`, `.stages{display:flex;gap:18px;padding:0;margin:0;list-style:none;color:var(--ms-faint);font-size:11px}.stages li{display:flex;align-items:center;gap:6px}.stages span{display:grid;place-items:center;width:20px;height:20px;border:1px solid var(--ms-line-strong);border-radius:50%;font:10px var(--ms-font-mono)}.stages .done{color:var(--ms-text)}.stages .done span{background:var(--ms-cue);border-color:var(--ms-cue);color:var(--ms-shell)}`);
