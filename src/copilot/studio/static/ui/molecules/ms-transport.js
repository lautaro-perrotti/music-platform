import { define, attr, emit } from '../lib/define.js'; import { icon } from '../lib/icons.js'; define('ms-transport', el => `<div class="transport"><button data-action="ms-play" aria-label="Play">${icon(attr(el,'playing')?'pause':'play',17)}</button><span class="time">${attr(el,'time','0:00')} / ${attr(el,'duration','2:41')}</span><ms-slider value="${attr(el,'progress','28')}" label="Playback position"></ms-slider><span class="tempo">${attr(el,'tempo','128')} BPM</span></div>`, `.transport{display:flex;align-items:center;gap:12px;padding:10px 14px;border-top:1px solid var(--ms-line);background:var(--ms-shell)}button{width:30px;height:30px;display:grid;place-items:center;border:1px solid var(--ms-line-strong);border-radius:50%;background:var(--ms-control)}.time,.tempo{font:11px var(--ms-font-mono);color:var(--ms-faint);white-space:nowrap}ms-slider{flex:1}`);
