import { defineLight, attr } from '../lib/define.js';
import { icon } from '../lib/icons.js';

const item = (route, label, glyph, active, key = '') => `<a class="claude-nav-item ${active === route ? 'active ' : ''}${key || route}" href="#${route}">${icon(glyph,17)}<span>${label}</span>${route === 'chat' ? '<kbd>/</kbd>' : ''}${route === 'create' ? '<kbd>G</kbd>' : ''}</a>`;
defineLight('ms-sidebar', el => { const active = attr(el,'active','home'); return `<nav class="claude-sidebar" aria-label="Main"><div>${item('projects','All projects','grid',active)}</div><div class="claude-section-label">${attr(el,'project','Rhythm Ashanti')}</div><div class="claude-nav-group">${item('home','Overview','wave',active)}${item('chat','Chat','wave',active)}${item('create','Create','plus',active)}${item('studio','Studio','wave',active)}${item('versions','Versions','wave',active)}${item('references','References','wave',active)}${item('stems','Stems','wave',active)}${item('voice','Voice','wave',active)}${item('mix','Mix / Master','wave',active)}</div><div class="claude-section-label">Work</div><div class="claude-nav-group">${item('jobs','Jobs','bolt',active)}<span class="claude-badge">2</span>${item('ableton','Ableton','wave',active)}<span class="claude-live-label">● Live</span>${item('activity','Activity','wave',active)}</div><div class="claude-sidebar-spacer"></div><div class="claude-system">${item('health','System health','wave',active)}${item('providers','Settings','wave',active)}<div class="claude-advanced"><span>Advanced mode</span><button role="switch" aria-checked="false" aria-label="Advanced mode"><i></i></button></div></div></nav>`; });
