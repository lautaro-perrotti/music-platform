import { define, attr } from '../lib/define.js'; define('ms-dialog', el => `<div class="dialog"><div class="head"><h3>${attr(el,'title','Confirm change')}</h3><button aria-label="Close">x</button></div><p>${attr(el,'message','This action creates a new version and preserves the original.')}</p><div><ms-button variant="ghost">Cancel</ms-button><ms-button variant="primary">Continue</ms-button></div></div>`, `.dialog{padding:20px;border:1px solid var(--ms-line-strong);border-radius:var(--ms-radius-dialog);background:var(--ms-panel);box-shadow:var(--ms-shadow-overlay)}h3{margin:0}.head{display:flex;justify-content:space-between}.head button{border:0;background:transparent;color:var(--ms-faint);font-size:20px}.dialog p{color:var(--ms-muted);line-height:1.5}.dialog>div:last-child{display:flex;justify-content:flex-end;gap:8px}`);
