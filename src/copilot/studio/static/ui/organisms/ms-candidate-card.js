import { defineLight, attr } from '../lib/define.js';
import { wf } from '../lib/waveform.js';

defineLight('ms-candidate-card', el => { const label = attr(el,'label','Candidate A'); const letter = label.slice(-1); const src = attr(el,'src',''); return `<article class="claude-candidate-card"><div class="claude-candidate-head"><span class="claude-candidate-key">${letter}</span><span class="claude-candidate-title">${label}</span><span class="claude-candidate-status">◆ Review required</span><span class="claude-candidate-duration">${attr(el,'duration','5:31')}</span></div><div class="claude-candidate-wave"><svg viewBox="0 0 1000 100" preserveAspectRatio="none"><path d="${wf(Number(attr(el,'seed','7')),{n:180})}"/></svg></div><div class="claude-candidate-actions"><button aria-label="Play ${label}" data-action="ms-play" class="claude-candidate-play">▶</button><button data-action="ms-keep" class="app-native-button">Keep</button><button data-action="go-compare" class="app-native-button">Compare</button><span class="claude-candidate-note">${attr(el,'rights','SIMULATED')}</span><button aria-label="More" class="claude-more">⋯</button></div>${src ? `<audio class="claude-audio" preload="none" src="${src}"></audio>` : ''}</article>`; });
