import { define, attr } from '../lib/define.js'; define('ms-health-row', el => `<div class="health"><span class="dot ${attr(el,'state','ok')}"></span><b>${attr(el,'label','Audio analysis')}</b><small>${attr(el,'detail','Available')}</small></div>`, `.health{display:flex;align-items:center;gap:9px;padding:10px 0;border-bottom:1px solid var(--ms-line)}.dot{width:8px;height:8px;border-radius:50%;background:var(--ms-success)}.blocked{background:var(--ms-warning)}.health small{margin-left:auto;color:var(--ms-faint);font:11px var(--ms-font-mono)}`);
