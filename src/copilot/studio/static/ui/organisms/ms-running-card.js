import { define, attr } from '../lib/define.js'; define('ms-running-card', el => `<section class="running"><ms-stage-list active="${attr(el,'active','2')}"></ms-stage-list><h3>${attr(el,'title','Generating candidates')}</h3><ms-status state="running" label="Generating audio - candidate 2"></ms-status><p>Stage-based status only. No fake percentage.</p></section>`, `.running{display:flex;flex-direction:column;gap:14px;padding:18px;border:1px solid color-mix(in srgb,var(--ms-cue) 40%,var(--ms-line));border-radius:var(--ms-radius-panel);background:color-mix(in srgb,var(--ms-cue) 5%,var(--ms-panel))}.running h3{margin:0;font-size:16px}.running p{margin:0;color:var(--ms-faint);font-size:12px}`);
