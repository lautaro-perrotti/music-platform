import { defineLight, attr } from '../lib/define.js';
import { icon } from '../lib/icons.js';

defineLight('ms-top-bar', el => `<header class="claude-topbar"><a class="claude-brand" href="#projects"><span class="claude-mark">M</span><span>Music Studio</span></a><div class="claude-crumb"><a href="#projects">${attr(el,'project','No project')}⌄</a><span>/</span><a href="#versions"><i></i>${attr(el,'version','No active version')}<b>Active</b></a></div><div class="claude-search"><a href="#providers">${icon('search',15)}<span>Search, jump, or run a command</span><kbd>Ctrl K</kbd></a></div><div class="claude-tools"><a href="#ableton" class="claude-tool live"><span></span>Live 12 <em>Read-only</em></a><a href="#providers" class="claude-tool">${icon('bolt',14)}Best available</a><a href="#jobs" class="claude-tool jobs">${icon('bolt',14)}${attr(el,'jobs','No jobs')}</a><button class="claude-notify" aria-label="Notifications">${icon('bell',17)}<i></i></button><span class="claude-avatar" aria-label="Lucas Perrotti">LP</span></div></header>`);
