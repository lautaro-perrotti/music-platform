# CAPABILITY_MATRIX_V1

Machine source: `python -m copilot.cli capabilities`

`LIVE_SESSION_READINESS_V1` is **VERIFIED / FROZEN**. Musical generalization is **unproven** until a second real song.

See `copilot.audio.capability_matrix_v1` and `docs/core/SUPPORTED_ENVELOPE_V1.md`.
